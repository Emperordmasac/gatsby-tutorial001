import React from "react";
import Layout from "../components/Layout";

const About = () => {
    return (
        <Layout
            title={`About Page`}
            content={`Hi there! I'm the proud creator of this site, which I built with Gatsby.`}
        />
    );
};

export default About;
